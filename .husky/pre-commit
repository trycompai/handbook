#!/bin/sh
# Regenerate content data if markdown files changed
if git diff --cached --name-only | grep -q "content/.*\.md"; then
  echo "📝 Markdown files changed, regenerating content data..."
  node scripts/build-content.mjs
  git add src/lib/content-data.json
fi